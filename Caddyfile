# Prod uses sslip.io wildcard DNS against the EC2 Elastic IP 3.139.241.196
# (dots swapped for dashes -> 3-139-241-196). Caddy gets Let's Encrypt certs
# automatically. If the Elastic IP ever changes, update both hostnames below.
# Once you own a real domain, just swap these two hostnames for it.

api.3-139-241-196.sslip.io {
    reverse_proxy api:8002
}

app.3-139-241-196.sslip.io {
    reverse_proxy web:3001
}
